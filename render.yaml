services:
  - type: web
    name: vicodathon
    env: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TICK_MINUTES
        value: "60"
